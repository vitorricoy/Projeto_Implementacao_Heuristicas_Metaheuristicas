#pragma once

#include <bits/stdc++.h>

using namespace std;

struct Entrada
{
    vector<int> demanda;
    vector<vector<int>> pesos;
    int capacidade;
    int numeroVeiculos;
    int numeroClientes;
};